
#include "System/Delegate.h"

namespace System
{
    // Base Delegate class implementation is header-only since it's abstract
    // All concrete implementations are in the template specializations
}
